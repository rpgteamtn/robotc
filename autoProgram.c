#pragma config(Hubs,  S1, HTMotor,  HTMotor,  HTMotor,  none)
#pragma config(Hubs,  S2, HTServo,  HTMotor,  none,     none)
#pragma config(Sensor, S3,     gyro,           sensorI2CHiTechnicGyro)
#pragma config(Sensor, S4,     smux,           sensorI2CCustom)
#pragma config(Motor,  mtr_S1_C1_1,     spinner,       tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_1,     leftFront,     tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C2_2,     leftBack,      tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_1,     liftRight,     tmotorTetrix, openLoop)
#pragma config(Motor,  mtr_S1_C3_2,     liftLeft,      tmotorTetrix, openLoop, reversed)
#pragma config(Motor,  mtr_S2_C2_1,     rightFront,    tmotorTetrix, openLoop, reversed)
#pragma config(Motor,  mtr_S2_C2_2,     rightBack,     tmotorTetrix, openLoop, reversed)
#pragma config(Servo,  srvo_S2_C1_6,    goalCapture,          tServoStandard)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#include "AutoProgramQuestions.c"
#include "autoKickstand.c"
#include "autoRamp.c"
#include "autoIR.c"
#include "autoTubes.c"

task main()
{
	setBluetoothOff();

	bool check = false;
	int timeDelay;
	bool RPGKickStand;
	bool RPGStart;
	bool RPGCenter;
	int RPGtubeDump;


	do
	{
		timeDelay = questionInt("Amount of delay", "left = -1 sec","right = +1 sec", 0 , 30);//asks the questions and sets RPGtime as the answer either true or false
		waitTillNoButton();
		eraseDisplay();

		RPGStart = questionBool("r u starting:","left = floor","right = ramp");
		waitTillNoButton();
		eraseDisplay();

		RPGKickStand = questionBool("do kickstand?","left = no","right = yes");
		waitTillNoButton();
		eraseDisplay();

		RPGCenter = questionBool("Center goal first?", "left = no", "right = Yes");
		waitTillNoButton();
		eraseDisplay();

		RPGtubeDump = questionInt("which tube First?", "left = lower hight(1 == 30)", "right = higher hight(3 = 90)", 1, 3);
		waitTillNoButton();
		eraseDisplay();




		string txtDelay;// = "delay = " + timeDelay
		sprintf(txtDelay, "delay = %i", timeDelay);

		string txtLocation;// = RPGStart
		sprintf(txtLocation, "location = %i", RPGStart);

		string txtKickstand;// = "kickstand = " + RPGKickStand
		sprintf(txtKickstand, "Kickstand = %i", RPGKickStand);

		string txtCenter;// = "Center = " + RPGtubeDump
		sprintf(txtCenter, "Center = %i", RPGKickStand);

		string txttubeDump;// = "tubeDump = " + RPGtubeDump
		sprintf(txttubeDump, "tumb to dump = %i", RPGKickStand);



repeatUntil(((nNxtButtonPressed == nxtGrey)||(nNxtButtonPressed == nxtRight)||(nNxtButtonPressed == nxtLeft)||(nNxtButtonPressed == nxtOrange)))
{
		displayCenteredTextLine(3, "your values are:");
		displayCenteredTextLine(4, txttubeDump);
		displayCenteredTextLine(5,  txtDelay);
		displayCenteredTextLine(6,  txtLocation);
		displayCenteredTextLine(7, txtKickstand);

		wait1Msec(3000);

		eraseDisplay();

		displayCenteredTextLine(0, "Are these values correct?");//Displays question
		displayCenteredTextLine(1, "right = yes"); // Displays instructions
		displayCenteredTextLine(2, "left = no"); // Displays instructions
		displayCenteredTextLine(4, txtCenter);//need new line
		displayCenteredTextLine(5, txttubeDump);//need new line
}
		check = lastQuestion();
		waitTillNoButton();
	}
	while (false == check);



	waitForStartOrButton();



	wait1Msec(timeDelay*1000);

	if(RPGStart == true)//starting on ramp
	{
		rampOff();
	}

	else if(RPGStart == false)// starting on Floor
	{
		if(RPGKickStand == true)
		{
			autoKickstand();
		}

		else
		{
			//move into location
		}
	}

	else
	{
	}

	if(RPGtubeDump == 1)
	{
		autoTubes(eshort);
	}

	else if(RPGtubeDump == 2)
	{
		autoTubes(emid);
	}

	else if(RPGtubeDump == 3)
	{
		autoTubes(etall);
	}

	else
	{}

}
